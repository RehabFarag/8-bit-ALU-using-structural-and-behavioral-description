module orGate #(parameter N = 8)(
					input wire [N-1:0] in1,
					input wire [N-1:0] in2,
					output wire [N-1:0] out);
					
	genvar i;
	generate
	for (i = 0; i < N; i = i + 1)
	begin: Or_gen
		or AG(out[i], in1[i], in2[i]);
	end
	endgenerate
	
endmodule
